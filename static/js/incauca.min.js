// script de tiempo de espera de la alerta de la aplicación web de sellos incauca
document.addEventListener('DOMContentLoaded', function () {
    setTimeout(function () {
    let alerts = document.querySelectorAll('.alert')
    alerts.forEach(function (alert) {
        alert.style.opacity = '0'
        setTimeout(function () {
        alert.remove()
        }, 2000)
    })
    }, 2000)
});
// Script para mostrar el modal de confirmación al eliminar un sello
document.querySelectorAll('.btn-outline-danger').forEach(function (button) {
    button.addEventListener('click', function (event) {
        event.preventDefault()
        let url = this.getAttribute('href')
         Swal({
            title: '¿Estás seguro?',
            text: "¡Vas a eliminar este sello!",
            icon: 'warning',
            showCancelButton: true,
            confirmButtonColor: '#3085d6',
            cancelButtonColor: '#d33',
            confirmButtonText: 'Sí, eliminar'
        }).then((result) => {
            if (result.isConfirmed) {
                window.location.href = url
            }
        })
    })
});
// Script para exportar la tabla a Excel
$(document).ready(function() {
    $("#btnExport").click(function() {
        $("#tabla").table2excel({
            exclude: ".noExl", // Exclude elements with this class
            name: "selloINC",
            filename: "sellos_incauca.xls", // Desired filename
            fileext: ".xls",
            exclude_img: true, // Exclude images
            exclude_links: true, // Exclude links
            exclude_inputs: true // Exclude form inputs
        });
    });
});